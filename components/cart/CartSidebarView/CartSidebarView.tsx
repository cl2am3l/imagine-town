import React from 'react'

interface Props {
    
}

const CartSidebarView = (props: Props) => {
    return (
        <div>
            CartSidebarView
        </div>
    )
}

export default CartSidebarView
